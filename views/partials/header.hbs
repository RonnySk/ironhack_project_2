<header>
    {{#if userFromSession}}
    <a class="logo" href="/flat">🥨</a>    
    <div class="btn-group">
        {{#if userIsAdmin}}
        <button class="toggle-edit-mode">Edit flat</button>
        {{/if}}
        <a href="/user/{{userFromSession.id}}" class="btn">User Settings</a>
        <form action="/logout" method="post">
            <button type="submit">Logout</button>
        </form>
    </div>
    {{else}}
    <a class="logo" href="/">🥨</a>    
    {{/if}}
</header>